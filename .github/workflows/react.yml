name: React

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  common:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 14

      - name: Install dependencies with npm ci
        run: |
          npm install
          ls -a
          ls -a ./frontend/

  build:
    needs: common
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2  # This checkout is necessary to get the correct context

      - name: Build React App
        working-directory: frontend/
        run: npm run build


  lint:
    needs: common
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2  # This checkout is necessary to get the correct context

      - name: Run ESLint
        working-directory: frontend/
        run: npx eslint .
